Bài cũ:
   Local host dùng để tham chiếu trực tiếp đến địa chỉ của máy(id của local host là 127.0.0.1)
   engine V8 biên dịch từ js-> c++ -> asembly
   express js là một frame work web dùng để xử lí api một cách nhanh chóng
   app.get('/', (request , response) => {
      res.send('Hello World!')
   })
Bài mới:
    even loop? call stack? callback queue?
    sever backend ... web sever...
    sever backend: được nhận reques từ websever gởi về, vd: google gởi request qua websever ... 
    qua sever backend(nhiều sever) , websever sẽ cân bằng tải và dùng các proxy để bảo vệ sever và client
    clone code thì sẽ clone package về rồi check dependency ...  
    router: API end point .... ??
    